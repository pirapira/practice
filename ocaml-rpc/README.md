This directory contains an example program that talks with a running eth instance.

I think I need to put this somewhere in this directory.
```
eth --test -d /tmp/test &
```


Opam dependencies:
`opam install rpc`
